1. UPDATE orders SET status = 'shipped' WHERE order_id = 1005;
10. CREATE TABLE logs (id INT PRIMARY KEY, action TEXT, created_at TIMESTAMP);
11. COMMIT;
12. SELECT * FROM orders WHERE customer_id IN (SELECT id FROM customers WHERE country = 'US');
13. REVOKE DELETE ON products FROM developer_role;
14. TRUNCATE TABLE session_data;
15. ROLLBACK;
16. SELECT name, COUNT(*) FROM orders GROUP BY customer_id HAVING COUNT(*) > 5;
17. INSERT INTO employees (name, salary) VALUES ('Alice', 75000), ('Bob', 68000);
18. ALTER TABLE users RENAME COLUMN old_name TO new_name;
19. CREATE VIEW active_users AS SELECT * FROM users WHERE last_login > '2024-01-01';
2. INSERT INTO products (name, price) VALUES ('Keyboard', 99.99);
20. EXPLAIN SELECT * FROM large_table WHERE category = 'Electronics';
3. ALTER TABLE employees ADD COLUMN department VARCHAR(50);
4. DELETE FROM customers WHERE last_login < '2023-01-01';
5. CREATE INDEX idx_email ON users (email);
6. SELECT AVG(salary) FROM employees WHERE department = 'Sales';
7. DROP TABLE IF EXISTS temp_logs;
8. GRANT SELECT ON customers TO analyst_role;
9. BEGIN TRANSACTION; UPDATE accounts SET balance = balance - 100 WHERE user_id = 1;
